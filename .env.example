# Copy to .env and fill in. .env is gitignored — never commit real values.
ORACLE_HOST=localhost
ORACLE_PORT=1521
ORACLE_SERVICE=FREEPDB1
ORACLE_USER=rc360
ORACLE_PASSWORD=
APP_USER_PASSWORD=

# generator scale — start small, raise once the pipeline runs end to end
N_CUSTOMERS=5000
N_MONTHS=12
RANDOM_SEED=42
